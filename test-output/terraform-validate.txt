$ terraform init -backend=false && terraform validate   # remediate/terraform/
Success! The configuration is valid.

$ trivy config --severity CRITICAL,HIGH --exit-code 1 .   # fixtures skipped
All targets clean — 0 misconfigurations.

$ gitleaks git --config .gitleaks.toml .   # full history, v8.21.2
no leaks found — 1 real first-run false positive (kubernetes-secret-yaml on the
ExternalSecret reference) waived by commit-independent regex through waivers.yaml (SEC-151)
